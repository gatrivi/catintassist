/** Single source of truth for app version (UI + package.json should match). */
export const APP_VERSION = '4.95.0';
export const APP_VERSION_LABEL = `v${APP_VERSION} - soundboard live-readiness: throttle-proof ramp, autoplay surfacing, mic-swap guard, normalize, crossfade`;
