# Vite — not used here

## Official
- https://vite.dev/

## This repo
**Not applicable.** Build/dev is CRA (`react-scripts`). Do not assume:
- `vite.config.js/ts`
- Vite `import.meta.env` conventions
- Vite plugin APIs

If a future migration happens, update this file and `react-cra.md` first.
